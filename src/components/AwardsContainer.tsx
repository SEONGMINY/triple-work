import styled from 'styled-components'

import { ANIMATION_STYLES } from '../style/common/animation'

const AwardsContainer = styled.div`
  display: flex;
  margin-top: 30px;
  ${ANIMATION_STYLES.fadeIn(200)}
`

export default AwardsContainer
